
protocol s2c 500
{
	AckGroupList( int errorCode, GroupVector groups );
	AckGroupJoin( int errorCode );
	AckGroupCreate( int errorCode, std::string groupName, netid groupid );

	AckP2PConnect( int errorCode, network::P2P_STATE state, std::string ip, int port );
	AckP2PHostCreate(  int errorCode );


	func1( );
	func2( std::string str );
	func3( float value );
	func4( );
	func5(std::string ok, float a, int b );
	
}

protocol c2s 600
{
	ReqGroupList( netid groupid );
	ReqGroupJoin( netid groupid );
	ReqGroupCreate( netid parentGroupId, std::string groupName  );

	ReqP2PConnect( );
	ReqP2PHostCreate(  bool isSuccess );

	func2( std::string str );
	func3( float value );
}

